import LoginForm from '@/app/ui/login-form';

export default function LoginPage() {

    return (
        <main className={`flex min-h-screen flex-col p-1 rounded-lg`}>
            <LoginForm />
        </main>
    );

}